# ── CodeBuddy — requirements.txt ─────────────────────────────────────
# Letting LangChain's own dependency resolver pick mutually-compatible
# versions instead of pinning each package separately. Exact pins on
# langchain-core/community/text-splitters/chroma kept causing conflicting
# version chains since they all depend on each other.
#
# Install:  pip install -r requirements.txt

# ── Core LangChain (no hard pin — let pip resolve compatible set) ─────
langchain-core>=1.1.3
langchain-text-splitters>=0.3.11
langchain-community>=0.3.23

# ── Vector store (Chroma) ─────────────────────────────────────────────
langchain-chroma>=1.1.0
chromadb>=1.0.20

# ── Gemini (LLM + Embeddings) ──────────────────────────────────────────
langchain-google-genai>=2.1.4
google-generativeai>=0.8.4

# ── App framework ──────────────────────────────────────────────────────
streamlit==1.45.1

# ── GitHub repo cloning ─────────────────────────────────────────────────
gitpython==3.1.44

# ── Env variable loading (.env file) ────────────────────────────────────
python-dotenv==1.0.1
